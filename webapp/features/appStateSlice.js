sap.ui.define([], function() {
    
    function appStateReducer (state = {a: "something"}, action) {
            switch (action.type) {
                default: 
                    return state
            }
        }

        return appStateReducer

});