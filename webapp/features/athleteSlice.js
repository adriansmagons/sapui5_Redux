sap.ui.define([], function() {
    
    function athleteReducer (state = {a: "something"}, action) {
            switch (action.type) {
                default: 
                    return state
            }
        }

        return athleteReducer

});