sap.ui.define([], function() {
    
    function athleteReducer (state = {}, action) {
            switch (action.type) {

                default: 
                    return state
            }
        }

        return athleteReducer

});